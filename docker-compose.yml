version: "3.9"

services:
  backend:
    build: ./backend
    restart: unless-stopped
    ports:
      - "3000:3000"

  frontend:
    build: ./frontend
    restart: unless-stopped
    ports:
      - "80:80"
